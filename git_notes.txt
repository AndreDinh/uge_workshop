master branch is a label that git creates for us when we inialize a repsoitotry

HEAD  is another label that git creates for us when we create ou first commit

Head  is  *not* a branch

# creates a new branch (label) called git-notes
git branch git notes

# shows us with branch is active git branch